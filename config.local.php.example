<?php
/**
 * Configuration locale - COPIEZ CE FICHIER EN config.local.php
 *
 * Ce fichier n'est pas versionné (voir .gitignore)
 * Personnalisez les valeurs ci-dessous selon votre environnement
 */

// Mode production (désactive l'affichage des erreurs)
define('DEBUG_MODE', false);

// Clé API Claude (Anthropic)
// Obtenez votre clé sur https://console.anthropic.com/
define('CLAUDE_API_KEY', 'sk-ant-api03-VOTRE-CLE-ICI');

// Clé secrète pour l'API (utilisée par l'extension Chrome)
// Générez une clé aléatoire sécurisée
define('API_SECRET_KEY', 'votre-cle-secrete-aleatoire');

// URL du site (sans slash final)
define('SITE_URL', 'https://votre-domaine.com');

// Token secret pour les appels cron via HTTP (cron-job.org)
// Générez un token aléatoire, ex: bin2hex(random_bytes(32))
define('CRON_SECRET_TOKEN', 'votre-token-cron-secret');

// Configuration Mailchimp (nécessaire pour la newsletter)
// API key : Account > Extras > API keys sur mailchimp.com
// List ID : Audience > Settings > Audience name and defaults
define('MAILCHIMP_API_KEY', 'votre-api-key-us21');
define('MAILCHIMP_LIST_ID', 'votre-audience-id');

// Configuration Matomo Analytics (plugin WP Matomo sur le même domaine)
// Les URLs pointent vers le plugin Matomo installé dans WordPress
define('MATOMO_TRACKER_URL', '//www.k1m.be/wp-content/plugins/matomo/app/matomo.php');
define('MATOMO_JS_URL', '//www.k1m.be/wp-content/uploads/matomo/matomo.js');
define('MATOMO_SITE_ID', '1');
define('MATOMO_COOKIE_DOMAIN', '*.www.k1m.be');

// Nom et description du site (optionnel)
// define('SITE_NAME', 'News - Mon Site');
// define('SITE_DESCRIPTION', 'Ma description personnalisée');
